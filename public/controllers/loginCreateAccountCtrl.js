app.controller("loginCreateAccountCtrl", function( $scope/*, some service here,  We will need to determine which.*/) {

$scope.loginTest = "This test is from the Login/CreateAccount Controller file from $scope";
$scope.createAccountTest = "This test is from the Login/CreateAccount Controller file from $scope";

});